name: Create PR from dev to main

on:
  push:
    branches:
      - dev

jobs:
  asint-pr-bot:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Check for existing PR
        id: check-for-existing-pr
        run: |
          PR_LIST_RESP=$(gh pr list --base main --head dev --json number)
          PR_NUMBER=$(echo "$PR_LIST_RESP" | jq -r '.[0].number')

          if [[ "$PR_NUMBER" =~ ^[0-9]+$ ]]; then
            echo "PR_EXISTS=true" >> $GITHUB_ENV
            echo "PR_NUMBER=$PR_NUMBER" >> $GITHUB_ENV
            echo "Existing PR found: '$PR_NUMBER'"
          else
            echo "PR_EXISTS=false" >> $GITHUB_ENV
            echo "No existing PR, Creating new PR"
          fi
        env:
            GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          
      - name: Create new PR
        id: create-pr
        if: env.PR_EXISTS == 'false'
        run: |
          echo "PR_CREATED=false" >> $GITHUB_ENV

          CREATE_PR_RESP=$(gh pr create --base main --head dev --title "Merge dev into main" --body "Auto-generated PR to merge changes from dev to main.")
          PR_NUMBER=$(echo "$CREATE_PR_RESP" | grep -oP '\d+$')
          
          echo "PR_NUMBER=$PR_NUMBER" >> $GITHUB_ENV
          echo "PR_CREATED=true" >> $GITHUB_ENV
          echo "New PR: '$PR_NUMBER'"
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      
      - name: Check for conflicts
        id: check-for-conflicts
        if: env.PR_EXISTS == 'true' || env.PR_CREATED == 'true'
        run: |
          PR_NUMBER=${{ env.PR_NUMBER }}
          PR_MERGEABLE_STATUS=$(gh pr view $PR_NUMBER --json mergeable --jq '.mergeable')
          
          echo "PR MERGABLE: '$PR_MERGEABLE_STATUS'"

          if [[ "$PR_MERGEABLE_STATUS" == "MERGEABLE" ]]; then
            echo "PR '$PR_NUMBER' is mergeable"
            echo "PR_MERGEABLE=true" >> $GITHUB_ENV
          else
            echo "PR '$PR_NUMBER' has conflicts"
            echo "PR_MERGEABLE=false" >> $GITHUB_ENV
            exit 1
          fi
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Merge PR
        id: merge-pr
        if: env.PR_MERGEABLE == 'true'
        run: |
          if gh pr merge ${{ env.PR_NUMBER }} --merge 
          then
            echo "Pull request #${{ env.PR_NUMBER }} merged successfully"
          else
            echo "Failed to merge pull request #${{ env.PR_NUMBER }}"
            exit 1
          fi
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      
      - name: Install CF & CDS CLI
        id: cds-cli
        run: |
          npm i -g @sap/cds-dk
          sudo apt-get install cf8-cli

      - name: Schema Deployment
        id: schema-deployment
        run: |
          cf login -a https://api.cf.eu10-004.hana.ondemand.com -u sharan.n@asint.net -p 3052@narahS -o asint-development -s Development
          cds version
